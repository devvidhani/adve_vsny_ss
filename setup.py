from setuptools import setup, find_packages

setup(
    name='adve_vsny_ss',
    version='0.1',
    packages=find_packages(),
    # install_requires=[
    #     # List your dependencies here
    # ],
)
